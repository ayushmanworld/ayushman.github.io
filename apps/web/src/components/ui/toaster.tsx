'use client'

/**
 * Toaster — renders toast notifications.
 * Full implementation delivered in Phase 2 (Frontend Foundation).
 * This stub satisfies the Providers import during Phase 0.
 */
export function Toaster() {
  return <div id="toast-portal" aria-live="polite" aria-atomic="true" />
}
